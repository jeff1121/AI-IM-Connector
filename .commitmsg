feat: v0.2.2 — 多媒體傳送改為臨時 URL 文字連結

版本：0.2.1 → 0.2.2

問題：
- AI 生成的圖片透過 IM 平台原生多媒體推送 API 傳送時，IM 平台需回來拉取圖片
- LINE 等平台在特定情況下拉取失敗，導致使用者看到空白圖片

解決方案：
- MessageRouter 不再將多媒體交由 Controller 透過 IM 原生推送 API 傳送
- 改將暫存 URL 直接附加於文字回應中（格式：🖼️ 圖片：https://...）
- 使用者在 IM 點擊連結 → 瀏覽器 GET /api/media/{id} → 直接取得圖片
- 不依賴 IM 平台的圖片拉取機制，相容性更高

修改檔案：
- Services/MessageRouter.cs — 多媒體 URL 附加於文字回應，清除 MediaContents 避免重複推送
- docker/.env — SYSTEM_PROMPT 值加上單引號防止 bash source 語法錯誤
- docker/.env.example — 同步新增 PUBLIC_BASE_URL、SYSTEM_PROMPT 欄位

文件更新（zh-TW）：
- README.md — 更新版本、訊息流程步驟 8、功能表、API 端點說明、安全性章節、變更紀錄
- Tasks.md — 新增第十三階段、更新變更紀錄

測試：45 項全通過
Docker Image：logicalis.azurecr.io/ai-connector/im-connector:0.2.2（amd64 + arm64）
